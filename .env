# API Gateway URL — all platform API calls go through the gateway
# Gateway proxies to auth-service (8092) and other microservices
VITE_API_URL=http://localhost:8091

# Auth UI service URL — platform login redirects here for SSO
VITE_AUTH_UI_URL=http://localhost:8098
