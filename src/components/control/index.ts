/**
 * Afrisinc Control — Reusable Component Library
 *
 * Import from this barrel file to access all shared control components.
 * Each component is documented with JSDoc and usage examples inside its file.
 *
 * Available:
 *   PageHeader   — Consistent title/subtitle/actions block for every page
 *   StatCard     — Metric display card with icon, value, and change delta
 *   StatusBadge  — Coloured pill for ticket status, priority, account status
 *   EmptyState   — Designed empty list/table state (never a blank screen)
 *   SectionLabel — Uppercase tracking label for grouping content
 *   UserAvatar   — Circular avatar with 2-letter initials
 */
export { PageHeader } from "./PageHeader";
export { StatCard } from "./StatCard";
export { StatusBadge } from "./StatusBadge";
export { EmptyState } from "./EmptyState";
export { SectionLabel } from "./SectionLabel";
export { UserAvatar } from "./UserAvatar";
